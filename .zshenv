# ── Environment (available to all shells, including non-interactive) ──
export PATH="$HOME/.local/bin:/opt/homebrew/share/google-cloud-sdk/bin:$PATH"
export EDITOR="code --wait"
export VISUAL="code --wait"

# Starship config — clean single-line prompt

format = "$directory$git_branch$git_status$python$nodejs$rust$golang$java$docker_context$cmd_duration$character"

# ── Directory ────────────────────────────────────────────
[directory]
truncation_length = 5
truncation_symbol = "…/"
style = "bold cyan"
read_only = " 󰌾"
home_symbol = ""
repo_root_style = "bold yellow"
before_repo_root_style = "dimmed white"
repo_root_format = "[$before_root_path]($before_repo_root_style)[$repo_root]($repo_root_style)[$path]($style)[$read_only]($read_only_style) "
format = "[$path]($style)[$read_only]($read_only_style) "
truncate_to_repo = true

# ── Git ──────────────────────────────────────────────────
[git_branch]
symbol = ""
style = "bold purple"
format = "[on $symbol $branch(:$remote_branch)]($style) "

[git_status]
ahead = "[⇡${count}](bold green)"
behind = "[⇣${count}](bold red)"
diverged = "[⇕⇡${ahead_count}⇣${behind_count}](bold yellow)"
conflicted = "[=${count}](bold red)"
untracked = "[?${count}](bold blue)"
stashed = "[*${count}](dimmed white)"
modified = "[!${count}](bold yellow)"
staged = "[+${count}](bold green)"
renamed = "[»${count}](bold cyan)"
deleted = "[✘${count}](bold red)"
format = '($all_status$ahead_behind )'

# ── Languages (only show when relevant) ──────────────────
[python]
symbol = " "
style = "bold yellow"
format = '[$symbol($virtualenv)]($style) '

[nodejs]
symbol = " "
style = "bold green"
format = '[$symbol]($style) '

[rust]
symbol = " "
style = "bold #f74c00"
format = '[$symbol]($style) '

[golang]
symbol = " "
style = "bold cyan"
format = '[$symbol]($style) '

[java]
symbol = " "
style = "bold #f89820"
format = '[$symbol]($style) '

[docker_context]
symbol = " "
style = "bold blue"
format = '[$symbol$context]($style) '

[gcloud]
disabled = true

# ── Fill (pushes right side content) ─────────────────────
[fill]
symbol = " "

# ── Right side ───────────────────────────────────────────
[cmd_duration]
min_time = 2_000
format = "[ $duration]($style) "
style = "bold yellow"

[time]
disabled = true

# ── Prompt character ─────────────────────────────────────
[character]
success_symbol = "[→](bold green)"
error_symbol = "[→](bold red)"
vimcmd_symbol = "[←](bold green)"
